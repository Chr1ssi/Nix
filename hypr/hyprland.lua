require("monitors")

require("environment")

require("appearance")

require("input")

require("keybinds")

require("rules")


hl.config({
    dwindle = {
        preserve_split = true,
    },
})


hl.config({
    master = {
        new_status = "master",
    },
})


hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
    },
})


hl.config({
    misc = {
        force_default_wallpaper = -1,
        disable_hyprland_logo = false,
    },
})
